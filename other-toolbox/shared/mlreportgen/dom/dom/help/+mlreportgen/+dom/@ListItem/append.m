%append Append an object to this list item
%    textObj = append(itemObj, 'text') appends a Text object constructed
%    from the specified text string to the list item.
%
%    textObj = append(itemObj, 'text', 'styleName') appends a Text object 
%    constructed from the specified text string and having the specified
%    style name to the list item.
%
%    textObj = append(itemObj, domObj) appends a DOM object to the item.
%    The object can be any of the following types:
%
%        * CustomElement
%        * ExternallLink
%        * Image
%        * InternalLink
%        * LineBreak
%        * NumPages
%        * Page
%        * PageRef
%        * Paragraph
%        * Table
%        * Text

%    Copyright 2014-2016 MathWorks, Inc.
%    Built-in function.
