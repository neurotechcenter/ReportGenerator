%append Append content to a table
%    rowObj = append(tableObj, rowObj) appends a row of table entries to 
%    the specified table.
%
%    customElemObj = append(elemObj, customElemObj) appends a 
%    CustomElement to this element.
%
%    Note: the custom element must be a valid HTML or DOCX child of this
%    table, depending on whether the output type of the document to
%    which this element is appended is HTML or DOCX, respectively.

%    Copyright 2013 MathWorks, Inc.
%    Built-in function.
