%append Append an object to this link
%    textObj = append(linkObj, 'text') appends a Text object constructed
%    from the specified text string to the link.
%
%    textObj = append(linkObj, 'text', 'styleName') appends a Text object 
%    constructed from the specified text string and having the specified
%    style name to the link.
%
%    textObj = append(linkObj, domObj) appends a DOM object to the item.
%    The object can be either of the following types:
%
%        * Image
%        * Text
%        * CustomElement

%    Copyright 2014 MathWorks, Inc.
%    Built-in function.
