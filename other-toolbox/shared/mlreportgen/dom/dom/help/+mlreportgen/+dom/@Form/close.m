% close Close a document
%    close(docObj) closes the document, docObj. Once a document is closed,
%    you can no longer append content to it. Closing the document outputs
%    any remaining content, for example, any remaining template text.