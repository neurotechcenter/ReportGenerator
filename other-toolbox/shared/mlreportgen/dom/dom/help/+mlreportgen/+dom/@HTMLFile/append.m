%append Append an HTML string, HTML object or another HTMLFile object to an HTMLFile object
%    htmlObj = append(htmlFileObj, htmlText) converts an HTML markup text
%    (htmlText) to a group of DOM objects and appends it to htmlFileObj.
%
%    htmlObj = append(htmlFileObj, htmlObj) appends an HTML object 
%    to this HTMLFile object and returns the appended HTML object. 
%
%    anHtmlFileObj = append(htmlFileObj, anHtmlFileObj) appends an HTMLFile object 
%    to this HTMLFile object and returns the appended HTMLFile object.

%    Copyright 2014 MathWorks, Inc.
%    Built-in function.
